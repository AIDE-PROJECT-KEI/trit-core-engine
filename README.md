# trit-core-engine
A deterministic ternary execution engine (Allow / Hold / Deny) for safe AI action. The core of AIDE OS.
# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Node (if UI uses JS)
node_modules/

# Python (if minimal engine example added)
__pycache__/
*.pyc

# Build artifacts
dist/
build/

# Editor settings
.vscode/
.idea/

# Environment variables
.env

